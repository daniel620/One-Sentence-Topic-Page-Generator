{% from "components/_cite.html.j2" import cite with context %}
<section class="block timeline-block">
  {% if section.title %}<h2>{{ section.title }}</h2>{% endif %}
  {% if section.summary %}<p class="summary">{{ section.summary }}</p>{% endif %}
  <ol class="timeline">
    {% for item in section.items %}
    <li class="tl-row">
      <div class="when">{{ item.label }}</div>
      <div class="what">
        <span class="head">{{ item.value }}</span>
        {% if item.detail %}<div class="detail">{{ item.detail }}</div>{% endif %}
        {{ cite(item) }}
      </div>
    </li>
    {% endfor %}
  </ol>
</section>
